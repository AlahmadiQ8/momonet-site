{
  "name": "mohammad-asad-website",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build:11ty": "eleventy",
    "build:webpack": "cd momonet-2021 && npm run production && cd .. && cp momonet-2021/build/tailwind.css build/",
    "build": "rm -rf build && npm run build:11ty && npm run build:webpack",
    "dev:11ty": "eleventy --serve"
  },
  "repository": {
    "type": "git"
  },
  "license": "MIT",
  "devDependencies": {
    "date-fns": "^2.14.0",
    "glob": "^7.1.6"
  },
  "dependencies": {
    "@11ty/eleventy": "^0.11.1"
  }
}
